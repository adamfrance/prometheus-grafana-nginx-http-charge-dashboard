server {
    listen 80;
    server_name localhost;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
    }

    # Configuration basique pour stub_status (cette partie fonctionne)
    location /stub_status {
        stub_status on;
        allow all;
    }
}